# ============================================
# LingBot-World Interactive App Dependencies
# ============================================

# Backend dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ============================================
# LingBot-World Model Dependencies
# (from https://github.com/Robbyant/lingbot-world)
# ============================================

# PyTorch (requires CUDA for GPU acceleration)
torch>=2.4.0
torchvision>=0.19.0
torchaudio

# Diffusion & Transformers
diffusers>=0.31.0
transformers>=4.49.0,<=4.51.3
tokenizers>=0.20.3
accelerate>=1.1.1

# Vision & Video
opencv-python>=4.9.0.80
imageio[ffmpeg]
imageio-ffmpeg

# Utilities
tqdm
easydict
ftfy
einops>=0.7.0
numpy>=1.23.5,<2
scipy
Pillow>=10.0.0
safetensors>=0.4.0

# HuggingFace Hub (for model downloads)
huggingface-hub>=0.20.0

# ============================================
# flash-attn (GPU acceleration)
# Must be installed separately:
#   pip install flash-attn --no-build-isolation
# ============================================

# Note: flash_attn is listed in official requirements but needs
# special installation due to CUDA compilation requirements.
# The setup script handles this automatically.
